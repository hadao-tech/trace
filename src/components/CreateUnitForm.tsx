"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export function CreateUnitForm({products}:{products:Array<{id:string;name:string}>}){const router=useRouter();const [msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body=Object.fromEntries(f);setMsg("Đang tạo mã bảo mật…");const r=await fetch("/api/units",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const data=await r.json();if(r.ok){setMsg(`Đã tạo token: ${data.token}`);router.push(`/admin/units/${data.id}`)}else setMsg(data.error)}return <form onSubmit={submit} className="card p-6 grid md:grid-cols-2 gap-4"><div><label className="label">Sản phẩm</label><select name="productId" className="input" required>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></div><div><label className="label">Serial</label><input name="serialNumber" className="input" required placeholder="AV-2026-0006"/></div><div><label className="label">Lô sản xuất</label><input name="batchNumber" className="input" required placeholder="BATCH-2608"/></div><div><label className="label">Ngày sản xuất</label><input name="manufacturedAt" type="date" className="input" required/></div><div><label className="label">Hạn sử dụng</label><input name="expiresAt" type="date" className="input"/></div><div><label className="label">Ngưỡng lượt quét</label><input name="scanThreshold" type="number" className="input" defaultValue="10" min="1"/></div><div className="md:col-span-2"><button className="btn btn-primary" type="submit">Tạo đơn vị & token ngẫu nhiên</button>{msg&&<span className="ml-3 text-sm font-mono">{msg}</span>}</div></form>}
