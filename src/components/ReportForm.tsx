"use client";
import { useState } from "react";
export function ReportForm({unitId}:{unitId:string}){const [open,setOpen]=useState(false);const [state,setState]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("Đang gửi…");const f=new FormData(e.currentTarget);const res=await fetch("/api/reports",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({unitId,reason:f.get("reason"),details:f.get("details"),contact:f.get("contact")})});setState(res.ok?"Đã gửi báo cáo. Cảm ơn bạn đã giúp bảo vệ cộng đồng.":"Gửi báo cáo thất bại.")};if(!open)return <button className="btn btn-secondary w-full" onClick={()=>setOpen(true)}>Báo cáo sản phẩm đáng ngờ</button>;return <form onSubmit={submit} className="space-y-3"><select name="reason" className="input" required defaultValue=""><option value="" disabled>Chọn lý do</option><option>Nghi ngờ hàng giả</option><option>Tem bị hỏng hoặc tái sử dụng</option><option>Thông tin không khớp</option></select><textarea name="details" className="input min-h-24" placeholder="Mô tả chi tiết"/><input name="contact" className="input" placeholder="Email/SĐT (không bắt buộc)"/><button className="btn btn-primary w-full" type="submit">Gửi báo cáo</button>{state&&<p className="text-sm">{state}</p>}</form>}
