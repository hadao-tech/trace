import Link from "next/link";
import { AlertTriangle, Boxes, Eye, ScanLine } from "lucide-react";
import { db } from "@/lib/db";
export default async function AdminPage(){const [products,units,scans,reports,recent]=await Promise.all([db.product.count(),db.productUnit.count(),db.verificationLog.count(),db.suspiciousReport.count({where:{status:"OPEN"}}),db.productUnit.findMany({take:8,orderBy:{createdAt:"desc"},include:{product:true,_count:{select:{verifications:true,reports:true}}}})]);const stats=[{Icon:Boxes,label:"Sản phẩm",value:products},{Icon:ScanLine,label:"Đơn vị",value:units},{Icon:Eye,label:"Lượt xác thực",value:scans},{Icon:AlertTriangle,label:"Báo cáo mở",value:reports}];return <main className="container-page py-10"><div className="flex flex-wrap justify-between gap-4 items-end"><div><p className="text-emerald-700 font-bold">CONTROL CENTER</p><h1 className="text-3xl font-black">Tổng quan quản trị</h1></div><Link href="/admin/units/new" className="btn btn-primary">Tạo đơn vị sản phẩm</Link></div><section className="grid grid-cols-2 lg:grid-cols-4 gap-4 mt-8">{stats.map(({Icon,label,value})=><div className="card p-5" key={label}><Icon className="text-emerald-700"/><p className="text-3xl font-black mt-3">{value}</p><p className="text-sm text-slate-500">{label}</p></div>)}</section><section className="card mt-6 overflow-hidden"><div className="p-5 border-b"><h2 className="text-xl font-black">Đơn vị sản phẩm gần đây</h2></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-4">Sản phẩm / Serial</th><th className="p-4">Token</th><th className="p-4">Trạng thái</th><th className="p-4">Quét</th><th className="p-4"></th></tr></thead><tbody>{recent.map(u=><tr className="border-t" key={u.id}><td className="p-4"><b>{u.product.name}</b><br/><span className="text-slate-500">{u.serialNumber}</span></td><td className="p-4 font-mono">{u.token}</td><td className="p-4">{u.status}</td><td className="p-4">{u._count.verifications}</td><td className="p-4"><Link className="text-emerald-700 font-bold" href={`/admin/units/${u.id}`}>Chi tiết →</Link></td></tr>)}</tbody></table></div></section></main>}
